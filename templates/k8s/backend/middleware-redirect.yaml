# ============================================
# 后端服务 HTTP 重定向 HTTPS Middleware 模板
# ============================================
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirectscheme-${APP_NAME}
  namespace: ${NAMESPACE}
spec:
  redirectScheme:
    scheme: https
    permanent: true